#!/bin/bash

function lets-work() {
  if [[ "$*" == *-teams* ]]
  then
    cd /Applications && open 'Microsoft Teams.app' && cd ~
  fi
  
  if [[ "$*" == *--instatoolz-web* ]]
  then
    cd desktop && cd NovaComp/Appsketiers/instatoolz-fe && code . && cd ~
    osascript -e 'tell app "Terminal"
        do script "cd desktop && cd NovaComp/Appsketiers/instatoolz-fe && yarn start" 
    end tell'
    return
  fi

  if [[ "$*" == *--instatoolz-app* ]]
  then
    cd desktop && cd NovaComp/Appsketiers/instatoolz-app && code . && cd ~
    osascript -e 'tell app "Terminal"
        do script "cd desktop && cd NovaComp/Appsketiers/instatoolz-app && yarn run ios" 
    end tell'
  fi

  if [[ "$*" == *--core-app* ]]
  then
    cd desktop && cd NovaComp/Appsketiers/core_app && code . && cd ~
    osascript -e 'tell app "Terminal"
        do script "cd desktop && cd NovaComp/Appsketiers/core_app && yarn run ios" 
    end tell'
  fi

  if [[ "$*" == *--swimspot-app* ]]
  then
    cd desktop && cd NovaComp/Appsketiers/swimspot-apps && code . && cd ~
    osascript -e 'tell app "Terminal"
        do script "cd desktop && cd NovaComp/Appsketiers/swimspot-apps && yarn run ios" 
    end tell'
  fi

}